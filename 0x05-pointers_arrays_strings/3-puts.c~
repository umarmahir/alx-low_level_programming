/**
 * _puts - Entry point
 *
 * Description: 'The function that prints strings to the stdout'
 *
 * @s: The string to br displayed
 * Return: Always 0 (Success)
 */

void _puts(char *s)
{
	while (*s != '\0')
	{
		_putchar(*s);
		s++;
	}
}
