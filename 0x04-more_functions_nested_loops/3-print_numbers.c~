/**
 * mul - Entry point
 *
 * Description: 'the program's description'
 *
 * @a: The first  number
 * @b: the second number
 *
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
