/**
 * _isupper - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int _isupper(int c)
{
	if (c > 65 && c < 91)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
