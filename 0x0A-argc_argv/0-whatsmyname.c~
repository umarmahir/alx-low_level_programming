#include "main.h"
/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char** argv)
{
	int i;

	for(i = 0;  i++)
	{
		_putchar();
	}
	_putchar("\n");
	return (0);
}
